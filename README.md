# A01
1.What data is used: California Housing dataset

2.How to run the script: install the requirements txt file
pip install -r requirements.txt,

change your working directory as needed to the A01 repo
cd path/to/A01, 

run the code
python src/boxplot.py

3.Expected output:The boxplot.py script was executed successfully, and the resulting boxplot was saved to figs/boxplot.png.
 
